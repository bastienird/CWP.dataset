% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarising_step.R
\name{summarising_step}
\alias{summarising_step}
\title{Summarising_step}
\usage{
summarising_step(
  main_dir,
  connectionDB,
  config,
  source_authoritylist = c("all", "IOTC", "WCPFC", "IATTC", "ICCAT", "CCSBT"),
  sizepdf = "long",
  savestep = FALSE,
  nameoutput = NULL,
  usesave = FALSE
)
}
\arguments{
\item{main_dir}{Character. The main directory containing the entities. (jobs/entities)}

\item{connectionDB}{Object. The database connection.}

\item{config}{List. Configuration list containing metadata and options for processing.}

\item{source_authoritylist}{Vector. Vector of source_authority to filter on, "all" being all of them.}

\item{sizepdf}{Character string. La taille peut prendre les valeurs suivantes :
\itemize{
\item \code{"long"} (par défaut) : Long with coverage.
\item \code{"middle"} : Long without coverage
\item \code{"short"} : Only first characteristics, first differences and main table of steps
}}

\item{savestep}{Logical TRUE/FALSE, should the .qs result of this be saved ?}

\item{nameoutput}{Character, name of the .qs if saved}

\item{usesave}{Logical Should we use the nameoutput .qs instead of rerunning everything ?}
}
\value{
NULL. The function has side effects, such as writing files and rendering reports.
}
\description{
This function performs various summarizing steps on data related to species and gear types, retrieving data from a database,
processing it, and rendering output reports.
}
\examples{
\dontrun{
connectionDB <- DBI::dbConnect(RSQLite::SQLite(), ":memory:") # Connexion temporaire
config <- list() # Simule une configuration
summarising_step(main_dir = "chemin/vers/dossier", connectionDB, config)
}
}
